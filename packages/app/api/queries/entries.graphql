query userCollection($userId: String!) {
    entries(userId: $userId) {
        imageUrl
        videoUrl
        description
        title
        id
        artist
        code
        issuer
    }
}