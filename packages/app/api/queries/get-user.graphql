query authenticatedUser {
  authenticatedUser {
    avatarUrl
    displayName
    email
    username
    id
    description
    jwt
    publicKey
    lastPlayedEntry {
      imageUrl
      videoUrl
      description
      title
      id
      artist
      code
      issuer
    }
  }
}
