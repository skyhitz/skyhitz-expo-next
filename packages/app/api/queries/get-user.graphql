query authenticatedUser {
  authenticatedUser {
    avatarUrl
    displayName
    email
    username
    id
    description
    jwt
    publicKey
    managed
    lastPlayedEntry {
      imageUrl
      videoUrl
      description
      title
      id
      artist
      code
      issuer
    }
  }
}
